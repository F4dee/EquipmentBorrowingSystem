server.port=8080

spring.application.name=equipmentborrowingsystem

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/equipment_borrowing_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
# TODO: Change this to your actual MySQL root password (e.g., admin, root, or leave blank if no password)
spring.datasource.password=JEJONAZZ2K23

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# CORS configuration to allow local React app
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
